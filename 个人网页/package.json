{
  "name": "academic-site-build",
  "version": "1.0.0",
  "scripts": {
    "build:posts": "node build_scripts/build_posts.js",
    "build:publications": "node build_scripts/build_pubs.js",
    "build": "npm run build:posts && npm run build:publications"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^5.1.1"
  }
}